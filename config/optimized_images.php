<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Bật nén / chuyển WebP khi upload
    |--------------------------------------------------------------------------
    */
    'enabled' => env('OPTIMIZED_IMAGES_ENABLED', true),

    /*
    |--------------------------------------------------------------------------
    | Chất lượng WebP (0–100)
    |--------------------------------------------------------------------------
    */
    'quality' => (int) env('OPTIMIZED_IMAGES_QUALITY', 82),

    /*
    |--------------------------------------------------------------------------
    | Giới hạn cạnh dài tối đa (px). Ảnh lớn hơn sẽ scale giữ tỷ lệ.
    |--------------------------------------------------------------------------
    */
    'max_width' => (int) env('OPTIMIZED_IMAGES_MAX_WIDTH', 2560),
    'max_height' => (int) env('OPTIMIZED_IMAGES_MAX_HEIGHT', 1920),

];
